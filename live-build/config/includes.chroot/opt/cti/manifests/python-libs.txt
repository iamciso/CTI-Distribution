# Librerías Python para scripting y notebooks, instaladas en /opt/cti/venv
# (venv con acceso a los paquetes del sistema: pandas, networkx, yara, shodan...).
# Formato: <paquete-pypi>[==version]
stix2
taxii2-client
pymisp
# pdfid se distribuye en PyPI como librería (sin console_script); /usr/local/bin/pdfid
# lo ejecuta como módulo desde este venv.
pdfid
